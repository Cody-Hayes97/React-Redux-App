{
  "version": 2,
  "name": "project-name",
  "builds": [
    {
      "src": "project-name/package.json",
      "use": "@now/static-build",
      "config": { "distDir": "client" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "project-name/$1" }]
}
